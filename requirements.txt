# Local Development Tracker — backend/data-service dependency inventory
#
# NOTE: This file is NOT used by the Next.js (wb-ldt-app-nepal) Posit Connect
# deployment. Connect resolves the Next.js app's dependencies from
# package.json / package-lock.json via its Node runtime, exactly the same
# way `npm install` would. This file only becomes relevant once the planned
# backend/data-service (AI brief cache/run store, ingestion jobs, document
# parsing, geospatial preprocessing, exports, MEGA migration support) is
# deployed as its OWN, separate Posit Connect content item (appmode
# "python-fastapi" or "python-api"), with its own manifest.json.
#
# Install locally with:
#   python -m pip install -r requirements.txt

# API service and configuration
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.20,<1.0

# Database, Supabase, and migrations
supabase>=2.10,<3.0
psycopg[binary,pool]>=3.2,<4.0
SQLAlchemy>=2.0,<3.0
alembic>=1.14,<2.0

# AI, retrieval, and resilient HTTP calls
openai>=1.59,<2.0
tiktoken>=0.8,<1.0
httpx>=0.28,<1.0
tenacity>=9.0,<10.0

# Data processing and country onboarding packages
pandas>=2.2,<3.0
numpy>=2.1,<3.0
pyarrow>=18.0,<21.0
openpyxl>=3.1,<4.0
xlsxwriter>=3.2,<4.0

# Geospatial preprocessing and validation
geopandas>=1.0,<2.0
shapely>=2.0,<3.0
pyproj>=3.7,<4.0
pyogrio>=0.10,<1.0
geojson>=3.1,<4.0

# Document parsing and source extraction
pypdf>=5.1,<6.0
pdfplumber>=0.11,<1.0
beautifulsoup4>=4.12,<5.0
lxml>=5.3,<6.0
markdownify>=0.14,<1.0

# Export generation
Jinja2>=3.1,<4.0
markdown>=3.7,<4.0

# Observability and structured serialization
structlog>=24.4,<26.0
orjson>=3.10,<4.0

# Backend testing and quality
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<1.0
respx>=0.22,<1.0
ruff>=0.8,<1.0
mypy>=1.13,<2.0

# ---------------------------------------------------------------------------
# Frontend (wb-ldt-app-nepal) — for reference only, authoritative source is
# package.json. Deployed separately on Posit Connect with a Node manifest.
# ---------------------------------------------------------------------------
#   @base-ui/react@^1.5.0
#   @supabase/ssr@^0.10.3
#   @supabase/supabase-js@^2.105.4
#   class-variance-authority@^0.7.1
#   clsx@^2.1.1
#   csv-parse@^6.2.1
#   d3-geo@^3.1.1
#   lucide-react@^1.16.0
#   maplibre-gl@^5.24.0
#   next@16.2.6
#   pdf-parse@^2.4.5
#   plotly.js@^3.5.1
#   proj4@^2.20.8
#   react@19.2.4
#   react-dom@19.2.4
#   react-plotly.js@^2.6.0
#   shadcn@^4.8.0
#   skillfish@^1.0.37
#   tailwind-merge@^3.6.0
#   tw-animate-css@^1.4.0
#   xlsx@^0.18.5